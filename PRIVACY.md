# プライバシーポリシー

最終更新日: 2026-09-21

Auto Form Filler（以下「本拡張」）は、日本語フォームの入力項目を Jev（TypeSafe AI の判定特化モデル）で判定し、端末内に保存したプロフィールから自動入力する Chrome 拡張です。本拡張は**個人情報の値を端末の外に一切送信しません**。この方針は本拡張の設計原則であり、例外を設けません。

## 1. 収集しない情報

本拡張は以下を収集・送信しません。

- プロフィールに登録した値（氏名・フリガナ・住所・電話番号・メールアドレス・生年月日・性別・会社名など）
- ページにすでに入力されている値（「空か埋まっているか」というフラグのみを扱い、値そのものは読みません）
- パスワード（`type="password"` のフィールドは観測段階で除外し、読み取りません）
- クレジットカード情報（`autocomplete="cc-*"` およびカード関連ラベルのフィールドは観測段階で除外します）
- ページの可視テキスト全文
- 個人を特定できる分析情報・行動履歴（テレメトリ・アナリティクスは組み込んでいません）

## 2. Jev API へ送信する情報

拡張の利用者が「このページに入力」を実行したとき、Jev API（利用者自身が選択・設定したプロバイダ: TypeSafe 直接 または OpenRouter）へ次の情報のみを送信します。

- フォームの各項目のメタデータ: ラベル、`name` 属性、`type`、`autocomplete`、`placeholder`、`required`、`maxlength`、直近の見出し、select/radio の選択肢（先頭数件）
- ページの URL（クエリ文字列・フラグメントを除いた origin + path）とページタイトル
- プロフィールの「項目名」とその説明（例: `family_name: "Family name / surname in kanji（姓・苗字）"`）。**値そのものは含みません**

プロフィールの値を送らずに「どの項目を入れるべきか」だけを Jev に判定させ、実際の値の当てはめ・入力は拡張内のコードが行います。

## 3. API キーの扱い

- 利用者自身が取得した Jev の API キーは、この端末の `chrome.storage.local` にのみ保存されます。
- API キーは Jev API 呼び出し時の `Authorization` ヘッダとしてのみ使用されます。
- プロフィールの JSON エクスポートには API キーを含めません。
- ログ・エラーメッセージには API キーを含めません。

## 4. 第三者への送信

Jev API 呼び出しの送信先は、利用者が「API 設定」で選択したプロバイダです。

- **TypeSafe 直接**: `api.typesafe.ai` へ送信されます。
- **OpenRouter 経由**: `openrouter.ai` へ送信され、OpenRouter を経由して TypeSafe のモデルが呼び出されます。

いずれの場合も、送信される内容は本ポリシーの「2. Jev API へ送信する情報」のとおりです。各プロバイダのプライバシーポリシー・利用規約は、それぞれの提供元（TypeSafe / OpenRouter）に従います。本拡張の開発者は、これらの第三者サービスにおけるデータの取り扱いについて責任を負いません。

## 5. ローカル保存の範囲

本拡張が保存する情報は、すべてこの端末の Chrome プロファイル内（`chrome.storage.local`）に留まります。

- プロフィール（氏名・住所・連絡先などの値、識別用の名前と色）
- API キー・プロバイダ設定・動作設定（確信度の閾値、ハイライト表示の有無など）
- 直近の実行結果（件数・レイテンシ・トークン数。値そのものは含みません）

`chrome.storage.sync`（Google アカウント経由でブラウザ間同期される領域）は**使用しません**。クラウドへのバックアップ・同期機能もありません。拡張をアンインストールすると、これらのデータは Chrome により削除されます。

## 6. 権限について

本拡張が要求する権限は次のとおりです。

| 権限 | 用途 |
|---|---|
| `activeTab` | 「このページに入力」を実行したときだけ、現在のタブにアクセスします |
| `scripting` | 実行時にのみ content script を注入します（ページに常駐しません） |
| `storage` | プロフィール・設定をこの端末に保存します |
| host_permissions（`api.typesafe.ai`, `openrouter.ai`） | Jev API の呼び出しに使います |

`<all_urls>` や `tabs` 権限は要求しません。ページを開いただけで自動的に動作することはなく、ポップアップのボタンまたはキーボードショートカットで明示的に実行した場合のみ動作します。

## 7. お問い合わせ

本ポリシーや本拡張のデータの取り扱いについてご質問がある場合は、GitHub リポジトリの Issue にてご連絡ください。

- リポジトリ: https://github.com/Yu-Rin-Chi2/auto-form-filler-plugin

## 8. 本ポリシーの変更

本拡張の機能追加・変更に伴い本ポリシーを更新することがあります。重要な変更がある場合は、リポジトリの CHANGELOG およびこのファイルの「最終更新日」で明示します。
